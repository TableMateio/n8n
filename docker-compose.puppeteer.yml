version: '3'

services:
  browser:
    image: browserless/chrome:latest
    ports:
      - '3000:3000'
    environment:
      - TOKEN=optional_security_token_if_needed
      - MAX_CONCURRENT_SESSIONS=10
      - CONNECTION_TIMEOUT=60000
      - PREBOOT_CHROME=true
    restart: unless-stopped
